---
description: Emit the brainstormed decision as a Gravity Well design doc
---
Emit our decisions as one or more design docs using the template in `${CLAUDE_PLUGIN_ROOT}/templates/design-doc-template.md` and place them in docs/design. Please see the instructions in that same file as well.

BEFORE writing anything: enumerate every decision reached in this conversation — the whole conversation, not just the most recent topic — and present that list to me, marking which doc each decision will land in. Wait for my confirmation of the scope split. Nothing decided may be dropped or moved to "out of scope" without my explicit say-so; if the decisions span multiple features, write multiple docs so every decision is captured in one of them.
